@using System.Drawing
@using WellsOperaticSociety.BusinessLogic
@using WellsOperaticSociety.Models
@inherits Umbraco.Web.Macros.PartialViewMacroPage

@{
    var dm = new DataManager();
    var functions = dm.GetFunctions(30, 0);
}
@functions{

    private string GetImage(Function function)
    {
        if (function.Image != 0)
            return Umbraco.TypedMedia(function.Image).GetCropUrl();
        return string.Empty;
    }

}
@if (functions.Any())
{
    <table class="table table-hover">
        <thead>
        <tr>
            <th></th>
            <th></th>
            <th></th>
        </tr>
        </thead>
        <tbody>
            @foreach (var function in functions)
            {
                <tr><td style="max-width: 300px;"><img class="img-responsive" src="@GetImage(function)"/></td><td>@function.Name</td><td><a class="btn btn-create" href="@Umbraco.TypedContent(1114).Url?id=@function.Id">Manage member roles</a></td></tr>
            }
        </tbody>
    </table>
}
else
{
    <p class="alert-info">Ther are no functions in the system yet for you to manage</p>
}